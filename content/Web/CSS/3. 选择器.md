常见的选择器有：元素选择器，id选择器，类选择器，
还有：属性选择器，伪类选择器 ，父子选择器等

### 类选择器

类选择是最常用的，因为他可以写入多个类名方便调用

可以用空格依次隔开所有的类名

### id选择器

id选择器只能选择一个样式，因此通常指定固定的样式

### 元素选择器

可以通过直接输入类似：p、h1 等样式直接选择，不常用

### 去掉默认边距

但是一般都会在开头写一段代码去掉默认的边距

```
*{
    margin: 0;padding: 0;
}
```

  这里的*是通配符用来设置网页的默认边距为0
