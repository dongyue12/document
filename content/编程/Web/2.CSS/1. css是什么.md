

### css简介

1. CSS（CascadingStyleSheets）是**层叠式样式表**的缩写，是一种样式语言，用于描述页面元素的呈现方式。
2. 它为我们提供了丰富的样式属性，从字体、颜色到布局和动画，让我们能够定制网页的外观和布局。
3. 它被广泛用于构建现代网页和APP，是构建UI的核心技术之一，与HTML和JavaScript一起组成了前端开发的三大支柱：

### HTML负责定义页面的结构；

1. avaScript负责处理交互和动态效果；
2. CSS则负责样式的呈现；
3. CSS并不需要特定的运行环境，而是由浏览器解释和执行，几乎所有现代的Web浏览器都支持CSS。

    5．CSS也不依赖其他编程语言，它可以直接嵌入HTML文档中，也可以作为外部样式表单独存在。
    6．CSS的上限很高，可以研究出很多复杂的样式。7．本视频只侧重CSS入门知识。